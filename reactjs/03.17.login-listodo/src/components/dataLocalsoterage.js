// export default function dataLocalsoterage(formvalue) {
//   let data = {};

// }
// Create a room for store
// const DataContext = createContext();

// const DatacontextProvider = (props) => {
//   const [formvalue, dispath] = useReducer(formvalue, []);

//   useEffect(() => {
//     if (Object.keys(!errors) && isSubmitting) {
//       localStorage.setItem('formValue', JSON.stringify(formValue))
//     }
//   }, [formValue]);
//   return (
//     <Provider value={{ formvalue, dispath }}>
//       {props.children}
//     </Provider>
//   )
// }

